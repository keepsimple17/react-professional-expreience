const gramsToPounds = grams => Math.round(grams * 0.00220462)

const meterToMiles = meters => Math.round(meters * 0.000621371)

export { gramsToPounds, meterToMiles }

export default {
  gramsToPounds,
  meterToMiles
}
