{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "mkdir -p build && cp -R $(ls | grep -E -v 'build|node_modules') build/"
  }
}
